<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Abstract — Tri-Tetra Theory (TTT / OOπ)</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,400;0,600;0,700;1,400&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #f7f5f0;
  --surface: #ffffff;
  --ink: #1a1f2e;
  --muted: #5a6270;
  --accent: #2d5a5a;
  --accent-weak: color-mix(in oklab, var(--accent) 10%, var(--surface));
  --admitted: #2d5a5a;
  --rejected: #8b3a3a;
  --open: #6b5b3a;
  --line: color-mix(in oklab, var(--ink) 10%, transparent);
  --step--1: clamp(0.85rem, 0.82rem + 0.15vw, 0.95rem);
  --step-0: clamp(0.95rem, 0.92rem + 0.2vw, 1.05rem);
  --step-1: clamp(1.15rem, 1.05rem + 0.5vw, 1.4rem);
  --step-2: clamp(1.4rem, 1.2rem + 1vw, 2rem);
  --step-3: clamp(1.8rem, 1.4rem + 2vw, 2.8rem);
  --space: clamp(1.2rem, 1rem + 1vw, 2rem);
  --radius: 10px;
  --shadow: 0 1px 3px rgba(0,0,0,0.04), 0 8px 20px -10px rgba(0,0,0,0.12);
}

* { box-sizing: border-box; margin: 0; }

body {
  font-family: "Inter", "Hiragino Sans", "Yu Gothic", "Noto Sans JP", sans-serif;
  background: var(--bg);
  color: var(--ink);
  line-height: 1.75;
  font-size: var(--step-0);
}

.container {
  max-width: 760px;
  margin: 0 auto;
  padding: calc(var(--space)*2) var(--space);
}

header {
  text-align: center;
  margin-bottom: calc(var(--space)*2.5);
  padding-bottom: calc(var(--space)*1.5);
  border-bottom: 1px solid var(--line);
}

header .meta {
  font-size: var(--step--1);
  color: var(--muted);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 0.6rem;
}

h1 {
  font-family: "Crimson Pro", "Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", serif;
  font-size: var(--step-3);
  font-weight: 700;
  line-height: 1.15;
  letter-spacing: -0.02em;
  color: var(--ink);
}

header .subtitle {
  font-family: "Crimson Pro", "Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", serif;
  font-size: var(--step-1);
  color: var(--muted);
  font-style: italic;
  margin-top: 0.5rem;
  line-height: 1.4;
}

h2 {
  font-family: "Crimson Pro", "Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", serif;
  font-size: var(--step-2);
  font-weight: 600;
  margin-top: calc(var(--space)*2);
  margin-bottom: var(--space);
  padding-bottom: 0.4rem;
  border-bottom: 2px solid var(--accent);
  display: inline-block;
}

h3 {
  font-family: "Crimson Pro", "Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", serif;
  font-size: var(--step-1);
  font-weight: 600;
  margin-top: calc(var(--space)*1.5);
  margin-bottom: calc(var(--space)*0.6);
  color: var(--accent);
}

p {
  margin-bottom: calc(var(--space)*0.9);
  text-align: justify;
  word-break: normal;
  overflow-wrap: anywhere;
  hyphenate-limit-chars: 6 3 2;
}

.lead {
  font-size: var(--step-1);
  line-height: 1.6;
  color: var(--ink);
  margin-bottom: calc(var(--space)*1.5);
  padding: calc(var(--space)*1.2);
  background: var(--surface);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  border-left: 4px solid var(--accent);
}

section {
  margin-bottom: calc(var(--space)*2);
}

.claim {
  background: var(--surface);
  border-radius: var(--radius);
  padding: calc(var(--space)*1.1);
  margin-bottom: calc(var(--space)*1.2);
  box-shadow: var(--shadow);
  border-left: 5px solid var(--line);
  position: relative;
}

.claim.admitted { border-left-color: var(--admitted); }
.claim.rejected { border-left-color: var(--rejected); }
.claim.open { border-left-color: var(--open); }

.claim-label {
  display: inline-block;
  font-size: var(--step--1);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 0.15em 0.7em;
  border-radius: 4px;
  margin-bottom: 0.6rem;
}

.claim.admitted .claim-label { background: color-mix(in oklab, var(--admitted) 12%, transparent); color: var(--admitted); }
.claim.rejected .claim-label { background: color-mix(in oklab, var(--rejected) 12%, transparent); color: var(--rejected); }
.claim.open .claim-label { background: color-mix(in oklab, var(--open) 12%, transparent); color: var(--open); }

.claim-title {
  font-family: "Crimson Pro", "Hiragino Mincho ProN", "Yu Mincho", "Noto Serif JP", serif;
  font-size: var(--step-1);
  font-weight: 600;
  margin-bottom: 0.5rem;
  line-height: 1.3;
}

.claim p:last-child { margin-bottom: 0; }

ol.claims-list {
  list-style: none;
  padding: 0;
  counter-reset: claim-counter;
}

ol.claims-list > li {
  counter-increment: claim-counter;
  position: relative;
  padding-left: 2.2rem;
  margin-bottom: calc(var(--space)*0.8);
}

ol.claims-list > li::before {
  content: "(" counter(claim-counter, lower-roman) ")";
  position: absolute;
  left: 0;
  top: 0.05em;
  font-weight: 600;
  color: var(--accent);
  font-size: var(--step--1);
  font-family: "Crimson Pro", serif;
}

.badge-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.6rem;
  margin: calc(var(--space)*1.2) 0;
}

.badge {
  font-size: var(--step--1);
  padding: 0.35em 0.9em;
  border-radius: 100px;
  background: var(--surface);
  border: 1px solid var(--line);
  color: var(--muted);
  font-weight: 500;
}

.badge strong { color: var(--ink); }

.repro {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: calc(var(--space)*0.9);
  margin-top: calc(var(--space)*2);
  font-size: var(--step--1);
  color: var(--muted);
}

.repro code {
  background: var(--bg);
  padding: 0.2em 0.5em;
  border-radius: 4px;
  font-family: "SF Mono", "Consolas", monospace;
  font-size: 0.9em;
  color: var(--ink);
}

footer {
  margin-top: calc(var(--space)*3);
  padding-top: var(--space);
  border-top: 1px solid var(--line);
  font-size: var(--step--1);
  color: var(--muted);
  text-align: center;
}

.math {
  font-family: "Crimson Pro", "Times New Roman", serif;
  font-style: italic;
}

@media print {
  body { background: #fff; }
  .claim { break-inside: avoid; }
  header { page-break-after: avoid; }
}

@page {
  size: A4;
  margin: 18mm;
}
</style>
</head>
<body>
<div class="container">

<header>
  <div class="meta">Abstract &middot; Zenodo Note (2026-09-20)</div>
  <h1>Tri-Tetra Theory (TTT / OOπ)</h1>
  <div class="subtitle">Seven machine-verified notes on a geometric framework for the electron and Standard Model fermion sector</div>
</header>

<section>
  <div class="lead">
    Seven machine-verified notes testing whether a geometric framework (Tri-Tetra Theory, TTT / OOπ) can reproduce features of the electron and of the Standard Model fermion sector. Each note pairs a Python script — every assertion passing, <strong>119 in total</strong> — with a falsification-oriented write-up carrying null controls and a "free-integer audit": a claim is admitted only when no adjustable integer or exponent was used to obtain it.
  </div>
</section>

<section>
  <h2>Relation to Preceding Work</h2>
  <p>This work continues <strong>10.5281/zenodo.22800265</strong> (2026-09-16, "Counter-Rotating Pairs, Light Propagation, Refractive Index, the f-Sum Rule and the Electron g-Factor"). The electron g-factor and the rotating-medium picture of space were treated there and are <strong>not re-claimed here</strong>: in note 02, <span class="math">g−2</span> enters only as a constraint re-evaluation (a composite electron clears it by a factor <strong>2.0×10⁸</strong>, protected by chiral symmetry, which forbids the <span class="math">m<sub>e</sub></span>-linear moment), not as a derivation of the g-factor.</p>
  <p><strong>What is new here:</strong> the seat structure of the 16; the multipole selection rule that decides which shape can rotate without radiating; the exact icosahedron construction from two pentagonal bipyramids; the lower bound on the compressed sector; and the rejection of the 17-particle count with null controls. The identity <span class="math">ω<sub>Z</sub>·(λ̄<sub>C</sub>/2) = c</span> is presented as a mass-cancelling algebraic identity; if the preceding note already states it, that passage should be read as a reference to it.</p>
</section>

<section>
  <h2>Admitted Claims <span style="font-size:0.6em;color:var(--muted);font-weight:400;">(0 free integers)</span></h2>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">Particle Count: 16 = 2⁵ (SO(10) spinor), not 17</div>
    <p>The often-quoted count of 17 elementary particles is a bookkeeping convention, not a structural invariant — six standard counting schemes give <strong>16/17/18/29/61/118</strong> — whereas the derived per-generation count is <strong>16 = 2⁵</strong>, the SO(10) spinor; 17 is not a closed-shell number (neither tetrahedral, centred-tetrahedral, octahedral, nor a Mackay magic number), and the only non-numerological 17 in crystallography, the 17 plane groups, has rotation-order partition (2,3,3,4,5), which does not match the Standard Model's (1,3,3,4,6).</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">The "1 + 15" Split Holds Exactly for a Generation</div>
    <p>Within the 16, the <span class="math">SU(3)×SU(2)</span> singlets are exactly <strong>{e<sup>c</sup>, ν<sub>R</sub>}</strong>; the unique seat neutral under the full gauge group is <strong>ν<sub>R</sub></strong>, and the electron occupies the single adjacent seat — so a proposed "1 + 15" split holds exactly, but for a generation and only under "neutral = fully gauge-neutral" (electric neutrality yields 2 + 14, because <span class="math">ν<sub>L</sub></span> is uncharged but weak-charged).</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title"><span class="math">ω<sub>Z</sub>·(λ̄<sub>C</sub>/2) = c</span> is a Mass-Cancelling Algebraic Identity</div>
    <p>A particle at rest of <strong>any mass</strong> rotates at exactly the speed of light at a radius of half its reduced Compton wavelength.</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">Icosahedral Charge Distribution and Multipole Radiation</div>
    <p>A 12-point icosahedral charge distribution has no anisotropic multipole below <span class="math">ℓ = 6</span>, and its <span class="math">ℓ = 6</span> content is <span class="math">m = 0</span> and <span class="math">m = ±5</span> only; rigid rotation about the 5-fold axis therefore radiates at <strong>5ω</strong>, and prolate ("rugby-ball") deformation does not remove it — only continuous axial symmetry does, and since <span class="math">ωR/c = 1</span> exactly, no multipole suppression is available.</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">Exact Icosahedron from Two Pentagonal Bipyramids</div>
    <p>Two pentagonal bipyramids sharing both apices with a <strong>36°</strong> relative twist give exactly the regular icosahedron (7 + 7 − 2 = 12 vertices, 30 edges, all vertices 5-valent, edge/circumradius = 1/sin 72°); J13 is achiral (<span class="math">D<sub>5h</sub></span>) and elongation along any symmetry axis preserves the inversion centre, so chirality requires a twist other than 0° or 36°.</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">Lorentz Contraction Cannot Let a Particle Thread a Gap</div>
    <p>Lorentz contraction acts only along the direction of motion, leaving the transverse cross-section invariant: <strong>speed cannot let a particle thread a gap</strong>.</p>
  </div>

  <div class="claim admitted">
    <div class="claim-label">Admitted</div>
    <div class="claim-title">Lower Bound on the Compressed Sector: &gt; 1.2×10¹² GeV</div>
    <p>With the lattice spacing fixed by adopting <span class="math">ℏ</span>, <span class="math">E<sub>a</sub>/m<sub>e</sub>c² = λ̄<sub>C</sub>/a = 1.99×10¹⁴</span> identically — light and large are one ratio — and every known particle, together with standard dark-matter candidates, has compression index <span class="math">T &lt; 10⁻⁶</span>, i.e. <span class="math">T = 0</span>; consequently the compressed sector lies entirely above <strong>1.2×10¹² GeV</strong>, a lower bound obtained from the <span class="math">n = 2</span> Lorentz-invariance-violation window, which places it in the superheavy (WIMPzilla) dark-matter band.</p>
  </div>
</section>

<section>
  <h2>Re-evaluated Constraints</h2>
  <p>Two constraints from the project's falsification ledger were re-evaluated and now pass:</p>
  <div class="badge-row">
    <span class="badge">Electron point-particle bound: <strong>7.7×10⁷</strong></span>
    <span class="badge">Electron <span class="math">g−2</span>: <strong>2.0×10⁸</strong></span>
  </div>
  <p>The latter is protected by <strong>chirality</strong>, the same property that forces the binary tetrahedral group <strong>2T</strong> over 2O and 2I. Lorentz-invariance violation remains the single binding constraint.</p>
</section>

<section>
  <h2>Rejected Claims</h2>

  <div class="claim rejected">
    <div class="claim-label">Rejected</div>
    <div class="claim-title">electron = pentagonal bipyramid J13</div>
    <p>It has 10 faces, and it is achiral.</p>
  </div>

  <div class="claim rejected">
    <div class="claim-label">Rejected</div>
    <div class="claim-title">17 assembled from the framework's own vocabulary</div>
    <p>2 free integers, and 100% of the integers in [10,40] are reachable in one operation.</p>
  </div>

  <div class="claim rejected">
    <div class="claim-label">Rejected</div>
    <div class="claim-title">"uncompressed space = dark matter"</div>
    <p>Equation of state <span class="math">w = 0</span> versus <span class="math">−1</span>, observationally separated.</p>
  </div>

  <div class="claim rejected">
    <div class="claim-label">Rejected</div>
    <div class="claim-title">Elongation as a source of chirality</div>
    <p>Not supported by the geometric analysis.</p>
  </div>
</section>

<section>
  <h2>Left Open</h2>

  <div class="claim open">
    <div class="claim-label">Open</div>
    <p>The twist angle that would produce chirality; the origin of the lattice spacing <span class="math">a</span> (a free parameter inside the 7-decade window <span class="math">1 ≤ a/ℓ<sub>P</sub> &lt; 1.02×10⁷</span>); the 12-fold shortfall in Lorentz-invariance violation; the vacuum-energy excess of <strong>10⁹⁰.⁶</strong> (the cosmological constant problem, not specific to this framework); and the need for two independent mass mechanisms.</p>
  </div>
</section>

<div class="repro">
  <strong>Reproducibility:</strong> <code>pip install -r requirements.txt && bash run_all.sh</code><br>
  No external data, no randomness, no network access. Text CC BY 4.0; scripts MIT.
</div>

<footer>
  Main text in Japanese; this abstract and metadata are in English.
</footer>

</div>
</body>
</html>
